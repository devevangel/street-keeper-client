# API base URL (default: http://localhost:8000/api/v1)
# VITE_API_BASE_URL=http://localhost:8000/api/v1

# GPX/map engine: v1 = legacy, v2 = edge-based (UserEdge). Set v2 to use engine-v2 map and analyze.
VITE_GPX_ENGINE=v2

ENV=development
